mybatis:
  type-aliases-package: pe.edu.upc.RsOperation.mappers
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}

---

spring:
  profiles: dev
name: dev-YAML
environment: dev
datasource:
  driver-class-name: com.mysql.jdbc.Driver
  jdbcUrl: jdbc:mysql://localhost:3306/dbappfinance?useSSL=false
  username: root
  password: 7289
  maximumPoolSize: 5
auth-security:
  token:
    secretCode: "SecretKeyToGenJWTs"
    expirationTime: 100_000
    tokenPrefix: "Bearer "
    requestString: "Authorization"
server:
  port: 8081
  servlet:
    context-path: /
banner: banner.txt

---

spring:
  profiles: test
name: test-YAML
environment: test
datasource:
  driver-class-name: com.mysql.jdbc.Driver
  jdbcUrl: jdbc:mysql://201.150.40.204/dbappfinance?useSSL=false
  username: admin_auth
  password: dpm-upc.2018
  maximumPoolSize: 5
auth-security:
  token:
    secretCode: "SecretKeyToGenJWTs"
    expirationTime: 100_000
    tokenPrefix: "Bearer "
    requestString: "Authorization"
banner: banner.txt